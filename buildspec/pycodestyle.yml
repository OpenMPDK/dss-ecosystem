version: 0.2

batch:
  fast-fail: false
  build-matrix:
    dynamic:
      env:
        variables:
          PROJECT:
            - dss_ai_benchmark
            - dss_client
            - dss_datamover

phases:
  build:
    commands:
      - pycodestyle $PROJECT --show-source --show-pep8
